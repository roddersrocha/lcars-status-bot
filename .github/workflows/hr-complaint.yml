name: Starfleet HR
on: { schedule: [cron: '20 */17 * * *'], workflow_dispatch: }
jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Complaint
        run: |
          COMPLAINTS=(
            "The replicator gave me decaf AGAIN."
            "Why is the holodeck always booked for 'private training'?"
            "Ensign Ricky keeps dying in my simulations."
            "The captain used my sonic shower for 47 minutes."
            "Tribbles in the Jeffries tubes. Again."
          )
          TEXT="Starfleet HR Complaint (Anonymous): \"$(shuf -e "${COMPLAINTS[@]}" -n1)\" 😤"
          echo "TWEET<<EOF" >> $GITHUB_ENV && echo "$TEXT" >> $GITHUB_ENV && echo "EOF" >> $GITHUB_ENV
      - name: Post
        env: {API_KEY: ${{secrets.TWITTER_API_KEY}}, ... }
        run: # [OAuth]
